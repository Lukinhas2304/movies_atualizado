import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    imageBanner: {
        width: '52%',
        height: 350,
        marginTop: 15,
        borderRadius: 10
    },
    
    textBanner: {
        fontSize: 30,
        color: "white",
        marginTop: 15,
        fontWeight: 'bold'
    },
})

export default styles;